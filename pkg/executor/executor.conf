# Safe defaults for local/dev (non-Nitro). For Nitro, use executor.nitro.conf.
CHANNEL_TYPE=tcp
EXECUTOR_PORT=4000
EXECUTOR_ADMIN_PORT=4001
EXECUTOR_IP_HOST=127.0.0.1
LOG_SERVER_IP_HOST=127.0.0.1
EXECUTOR_LOG_NETWORK_LEVEL=info

# KMS disabled by default (dev/CI safe)
EXECUTOR_KMS_ENABLED=false
